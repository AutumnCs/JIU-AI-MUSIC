# PostgreSQL-compatible storage connection string for the future database adapter.
DATABASE_URL=

# Long random secret used to sign HTTP-only Jiu session cookies.
AUTH_COOKIE_SECRET=

# Optional local-only fallback for smoke tests.
JIU_ALLOW_LOCAL_AUTH_STORE=
